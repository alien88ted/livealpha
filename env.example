# Twitter API Configuration
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# Twitter Plan (affects rate limits): basic, pro, enterprise
TWITTER_PLAN=pro

# Database Configuration
DATABASE_URL=mysql://username:password@host:port/database_name

# Server Configuration
PORT=3000
HOST=localhost

# Twitter Accounts to Track (comma-separated)
TWITTER_ACCOUNTS=cz_binance,CookerFlips,ShockedJS

# Test Accounts (live feed only, not saved to database)
TWITTER_TEST_ACCOUNTS=alien88ted

# Environment
NODE_ENV=development

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info
